language: r

dependencies:
  - r
  - r-base
  - r-essentials
  - r-devtools
  
runtime:
  working_dir: ""
  command: |
    R -e "IRkernel::installspec()"
    jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root

include:
  - images/
  - nim_functions.R
